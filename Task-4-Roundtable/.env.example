# Copy this file to .env and fill in the blanks before running docker compose.

POSTGRES_USER=roundtable
POSTGRES_PASSWORD=
POSTGRES_DB=roundtabledb

# The hostname "db" is the compose service name. Use localhost outside Docker.
DATABASE_URL=postgresql://roundtable:CHANGE_ME@db:5432/roundtabledb

# Signs the session cookie. Generate with: openssl rand -hex 32
JWT_SECRET=

PORT=4003
